# Core dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
funasr>=1.3.1,<1.4
funasr-onnx==0.4.1
modelscope>=1.33.0
torch>=2.0.0
torchaudio>=2.0.0
httpx>=0.20.0

# Speaker diarization requires scikit-learn (indirect dependency).
# Python 3.13+ needs NumPy 2 wheels; current pandas/sklearn support them.
numpy>=1.20,<=1.26.4; python_version < "3.13"
numpy>=2,<3; python_version >= "3.13"
pandas>=1.3,<3
scikit-learn>=1.0,<2

# Video slide extraction (OpenCV scene detection; compatible with MLX Audio's click dependency)
opencv-python>=4.8
imagehash>=4.3.1
