# 变更历史

## [0.4.1] - 2026-09-23

### 修复

- 调整 C 封面三栏信息与页脚横线位置，使其更接近 designer HTML 模板；完整版附页改为独立分节与对应页名，正文页码从 1 起算。
- 无序列表保留圆点，避免被误排为有序编号；引文改为带灰底和内边距的卡片。

### 改进

- 正文行距和标题间距对齐 HTML `.prose` 规则；新增匿名回归测试，检查生成 DOCX 的列表、引文和分节页脚结构。
- 记录 Word 与 HTML/PDF 的正文总页数字段差异及跨引擎验收范围。

## [0.4.0] - 2026-09-23

### 新增

- 将 designer 同款棕色视觉体系的 Word OOXML 组件并入 generator：C/D/E/F 四款封面、正文标题与表格、页脚、落款及可选团队、裁判文书、律所和荣誉页。
- 新增通用 `word/render.js`，从逐案 Markdown 或结构化 JSON 生成可编辑 DOCX；完整版按选定律师读取本地 `team-config.md`，不再使用写死的案例内容。
- 保留 token 化 OOXML 模板与 `fill.py`，用于固定正文、少量变量的快速填充。

### 改进

- 明确对外方案的 Word 主交付路径和 designer 的 HTML/PDF 设计交付边界；公开文档说明依赖、输入契约与跨引擎验收。
- 多列表格固定布局，减少不同 Word 引擎的列宽漂移；缺失团队图片或选定律师时直接报错，避免生成看似完整的附页。

### 待办事项

- 在实际 WPS 和 Microsoft Word 中复核四款封面及后部附页的跨引擎表现；当前自动验收覆盖 DOCX 结构与 LibreOffice PDF。

## [0.3.2] - 2026-08-25

### 变更

- `SKILL.md` frontmatter 新增 `disable-model-invocation: true`：明确该 skill 需用户主动调用（`/` 触发），禁止模型在普通对话中自动生成法律文书，降低误生成风险。
- frontmatter `version` 由带引号 `"0.3.1"` 统一为无引号 `0.3.1`，与其它 skill 一致。

## [0.3.1] - 2026-07-15

### 新增写作原则：避免预承诺可量化交付物（自缚手脚防护）

- 通用写作要求新增第 5 条：方案中**不得把后续"可能但非必经"的工作写成具体的、可被客户量化追踪的交付物清单**（如各类评估报告、应对预案、答复意见框架等具名文件）。
- **背景**：在某知识产权维权方案初稿中，"应对对方反制程序"一段预列了多份具体文件，律师指出这些一旦写入会变成客户对律师的交付要求，自缚手脚、徒增工作量；且该反制程序是否发生取决于对方行为，本就不确定。
- **正确写法**：只提示风险 + 律师初步评估 + "如实际发生，作为独立项目另行委托、专项应对，费用届时协商"。适用于专利无效应对、反诉、二审/再审、执行异议、追加当事人等一切取决于对方行为/程序进展的后续工作。

## [0.3.0] - 2026-05-18

### 改进
- 重构步骤1"文档类型判断"的决策逻辑，解决"法律服务建议书"与"诉讼/非诉服务方案"的误匹配问题
- 新增优先级规则：案件性质（诉讼/非诉）为首要判断因素，用户措辞（"方案"≠"建议书"）为第二道防线
- 新增禁止规则：用户说"方案"时禁止自动降级为"建议书"，"建议书"仅在用户明确要求时生成
- 更新决策表排序：诉讼方案和非诉方案提到最前，建议书降至第三行并标注"用户明确要求"作为触发条件
- 更新关键判断因素：案件性质提升为首要因素，新增"用户措辞"判断维度

### 背景
实际使用中发现，当用户说"生成法律服务方案"时，AI反复匹配到"法律服务建议书"模板。根因是三个对外方案模板（诉讼服务方案、非诉服务方案、法律服务建议书）在命名和决策标准上存在重叠——"法律服务建议书"的名称包含"法律服务"，与用户通用说法"法律服务方案"词面最近，导致优先命中。而三者真正的区分维度是：业务类型（诉讼/非诉）× 客户场景（争取签约/已委托服务），建议书不应与两个方案模板平级竞争，而应作为仅限明确触发的独立文档类型。

## [0.2.0] - 2026-04-10

### 新增
- 新增“结案汇报”文档类型，用于案件以判决、调解、和解、撤诉等方式结案后向客户输出结果汇报
- 新增 `templates/case-closing-report.md`，沉淀授权条件对比、执行情况、客户实际支出和后续提醒等模块
- 新增技能级 `TASKS.md` 与 `DECISIONS.md`，补齐后续维护所需协作文档

### 改进
- 更新 `SKILL.md` 的文档类型清单、适用场景和选择逻辑，将结案收口场景纳入统一入口
- 更新 `references/modules.md`，新增“结案收口模块”和文档类型映射

### 文档完善
- 更新根目录 `README.md` 与 `.claude-plugin/marketplace.json` 中的技能描述与版本号，反映结案汇报能力已并入

### 待办事项
- 用至少 2 个真实结案案件验证模板是否覆盖判决结案与调解/和解结案两类场景
- 评估是否需要为执行结案、撤诉结案增加更细的可选模块

## [0.1.0] - 2026-02-07

### 新增
- 从 5 个法律服务文档模板整合为统一技能
- 支持诉讼服务方案、咨询报告、非诉方案、建议书、沟通报告 5 种文档类型
- 采用模块化架构，10 个核心模块可复用组合

### 技术优化
- 遵循 AGENTS.md 和 SKILL-GUIDE.md 规范
- Progressive Disclosure 设计：SKILL.md 简洁，详细信息在 references/

## 原始版本记录（test/archive 模板）

### 法律服务方案生成
- 基础模板：诉讼类服务方案
- 4部分结构：纠纷基本情况、难点重点、法律分析、服务方案

### 法律咨询报告生成提示词
- 适用场景：初次法律咨询
- 特点：含证据收集清单（表格+checkbox）

### 非诉法律服务方案模板
- 适用场景：非诉项目规划
- 特点：分阶段、模块化服务

### 非诉法律服务建议书模板
- 适用场景：服务方案推荐
- 特点：说服性较强

### 客户沟通报告模板与提示词
- 适用场景：综合沟通记录
- 特点：12部分完整结构，含成本效益分析
