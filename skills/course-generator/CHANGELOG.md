# 变更日志

## [2.9.15] - 2026-09-04

### 改进

- **把权威修订句改成可读规则卡**：`scaffold` 继续逐字注入 `COR-xxx.revised_text`，但统一放入 `> **关键规则：**` 引用块，避免弱模型因不得改写脚手架而把修订句保留成悬空断句；章节提示要求围绕规则卡自然展开，不复制第二份正文。
- **收紧单样本范围外推**：章节与总览提示明确，单个仓库、一次运行、一个团队或一张截图中的数字只能写成样本事实，不得扩成“常见规模”“普遍适用”“行业惯例”“标准做法”或“属于常态”。最终验证器按目标小节绑定来源块复核同族高风险表达，来源没有同等范围判断时归入 `CG-CLAIM-FIDELITY`。

### MiniMax 前向验证

- 冻结 v2.9.14 的 Round 10 使用全新 MiniMax-M3 任务完整生成内部课总览与 8 章正文，覆盖 197/197 个 content block，形成 96 项素材、28 个有来源小节、78 张完整图片账本与 6 张正文代表图；worker 正常结束，主模型重跑冻结版 preflight、八章检查和最终 15/15 全部通过。
- 主模型逐章复核后判断：课程已完整保留配置契约、依赖、JSON 中间件、change log/SemVer、Monorepo 与安全、Git 过程上下文、decisions/tasks、多 Agent、额度与法律服务产品化等高价值内容；21 个 `authority_superseded` 来源块未造成核心案例净丢失。相较 v2.9.13 的补字数、半成品和来源外扩写，正文完整度与完成率明显改善。
- 本轮仍有两项局部缺陷：10 条权威修订句被脚手架保留为视觉上突兀的独立段；“两千多个 PR、一百多贡献者”的单一仓库观察被写成“常见规模”。因此记 `PRODUCER_COMPLETED + FROZEN_MECHANICAL_PASS + SEMANTIC_PASS_WITH_FINDINGS`，可作为首个有效 v2.9.14 样本，但不足以声明弱模型稳定。
- v2.9.15 直接固化上述两项真实发现；用新版验证器重放 Round 10，精确命中“常见规模”一处 `CG-CLAIM-FIDELITY`，其余 14 项继续通过。新的 v2.9.15 MiniMax 前向样本形成前，效果仍为 `NOT_VERIFIED`。
- 冻结 v2.9.15 后启动全新 MiniMax-M3 Round 11 无锡课任务。Session 连续 1,838,933 ms 未取得首响应，始终只有 1 条任务消息、0 input/output token、0 个候选文件，随后调度器明确返回 `ConnectionRefused` 并把任务转为 `blocked`。本轮记 `PROVIDER_NO_FIRST_RESPONSE`、`skill_result=null`，不计 v2.9.15 成败；它说明当前失败来自 MiniMax 连接层，不是额度 429 或 Skill 执行轨迹。

### 技术优化

- 新增“来源外行业惯例”最小反例与“来源明确支持行业惯例”合法近似正例；索引器 15/15、manifest 收口 8/8、账本工具 32/32、账本预检 21/21、最终验证器 67/67，共 143/143 通过。manifest schema 继续使用 `1.8`，source-index schema 继续使用 `1.4`。
- 官方 `quick_validate.py` 仍因本项目按 ClawHub 规范保留 `version`、`author`、`homepage` 顶层字段而返回兼容性失败；这些字段是项目明确要求的元数据，未为迁就官方最小 frontmatter 白名单而删除。该结果不计入功能回归通过项。

## [2.9.14] - 2026-09-04

### 修复

- **移除诱发凑字数的全局深度线**：取消“全部章节合计不少于纳入来源 55%”的机械要求，继续保留单章 40% 异常下限、逐素材证据与单章 2.5 倍来源密度上限。章节提示明确禁止计算“还差多少字”、逐轮追加通用段落或刚好跨线。
- **阻断素材账本过度碎片化**：`status` 新增 `maximum_material_count`，`merge`、preflight 和最终验证统一限制整份账本不超过 `max(60, ceil(content block 数 × 0.5))`；要求合并同一观点、连续操作阶段和同一跳过理由，避免弱模型为每个发言片段建立素材后耗尽生成预算。
- **扩展来源框架与不确定事实边界**：书稿化门禁新增“来自现场实录”“演示一开始”“这一轮体验里”“回过头看”“整门课程到这里”等实际逃逸变体；提示明确保留“我记得/好像/可能/比如/畅想”等置信语气，不得从断裂 ASR 中挑出醒目身份、数字或平台名事实化。

### MiniMax 前向验证

- 冻结 v2.9.13 后完成两个全新 MiniMax-M3 任务。无锡课完整生成并通过冻结版 preflight、全部单章门禁和最终 15/15，但主模型人工验收仍硬失败：任务轨迹明确出现“还需 3180”“仍需 1581”“只差 1 个字符”等按全局 55% 线补写行为，成稿仍有来源框架、ASR 事实化及“全程无需干预”等来源外绝对化承诺。
- 内部课任务被平台标记成功，但约 45 分钟后只完成 3/7 章且缺总览；197 个 content block 被拆成 160 项素材，最终命中输出不完整、证据、深度和图片门禁，记为 `PRODUCER_INCOMPLETE_FALSE_SUCCESS`。这不是可用于评价正文语义质量的完整样本。
- v2.9.14 由上述两条可观察失败直接形成；新的冻结版弱模型前向生产完成前，效果继续标记 `NOT_VERIFIED`。
- 冻结 v2.9.14 后并行启动内部课与无锡课两个全新 MiniMax-M3 任务。两者分别运行约 416 秒和 308 秒后均在首个长响应末尾触发 `429 Token Plan (2056)`：内部课只完成索引/初始化，无锡课只完成 6 章计划且首批素材尚未成功 merge。两轮均记 `QUOTA_BLOCKED_DURING_FIRST_TURN`、`skill_result=null`，不计 Skill 成败；证据见外部 `evaluation/v2.9.14/minimax-rounds-8-9.md`。

### 技术优化

- 新增素材总数预算、真实来源框架变体的故障注入，并加入“单章 40%—55% 之间应合法通过”的反 Goodhart 正例；索引器 15/15、manifest 收口 8/8、账本工具 32/32、账本预检 21/21、最终验证器 65/65，共 141/141 通过。Harness 失效审查 0 finding；安全扫描无 high/critical，1 项 medium 为自测脚本使用 `subprocess.run(..., shell=False)` 的既有提示。manifest schema 继续使用 `1.8`，source-index schema 继续使用 `1.4`。

## [2.9.13] - 2026-09-04

### 新增

- **来源密度上限**：单章可见正文新增 `max(1400 字, 本章纳入来源字符数 × 2.5)` 异常上限，阻断弱模型在稀薄来源上用常识、元叙述或重复段落扩写；超过时应合并薄章或删除来源外内容。
- **中文成品质量门禁**：最终验证器新增中文语境半角标点、未闭合引号和高度近重复长段检查；把“写得像书稿”转成可定位的失败项。
- **ASR 异常处理规则**：大纲和章节提示明确要求语法断裂、疑似专名/数字误识别且上下文不能确认的片段不得照抄为事实，只允许最小可确认修正，否则舍弃或在审计中标待核。

### 修复

- **补齐占位审计标签逃逸**：审计隔离同时识别真实编号和 `SRC-xxx`、`BLK-xxxxx` 等占位形式，并拒绝“面向门禁”“生成过程术语”“读者正文审计”等元叙述。
- **阻断篇幅门禁 Goodhart**：保留章节/全局最低深度，但不再允许用近义重复或与来源无关的延伸刚好跨线；篇幅下限与来源密度上限共同定位异常，不作为写作目标。

### MiniMax 前向验证

- 冻结 v2.9.12 后完成两次全新 MiniMax-M3 单次生产。内部课与无锡课最终均通过冻结版 15/15 机械门禁、preflight 和临时文件检查，证明 v2.9.12 已明显改善流程完成率；但主模型人工验收仍判定 0/2 可交付。
- 内部课出现来源稀薄章节 6.46 倍扩写、ASR 噪声事实化和 `SRC-xxx`/“面向门禁”泄漏；无锡课出现全篇半角标点、重复段、未闭合引号、刚好跨过深度下限的补写，以及来源外绝对化承诺。两轮均记 `PRODUCER_COMPLETED_FROZEN_MECHANICAL_PASS_SEMANTIC_HARD_FAIL`。
- v2.9.13 的新门禁重放上述候选后均按预期失败：内部课命中来源密度与审计隔离，无锡课命中书面化质量。新的冻结 MiniMax 前向生产尚未执行，效果继续标记 `NOT_VERIFIED`。

### 技术优化

- 最终验证器故障注入增加半角标点、未闭合引号、近重复长段、来源过度扩写和占位审计 ID；索引器 15/15、账本工具 31/31、账本预检 20/20、manifest 收口 8/8、最终验证器 63/63，共 137/137 通过。

### 兼容性说明

- manifest schema 继续使用 `1.8`，source-index schema 继续使用 `1.4`；本次只提高生成规则和验收强度，不要求重建既有账本结构。

## [2.9.12] - 2026-09-03

### 新增

- **候选审查证据契约**：manifest schema 升级为 `1.8`；每条 `candidate_block_reviews` 新增至少 6 字、逐字来自对应候选预览的 `evidence_quote`，并阻断同一修正下批量复制理由及 12 项以上矩阵全部判为 `superseded`。
- **高级章节覆盖隔离**：普通生成路径继续硬限制 8 章，高级上限命令移入 `references/advanced-overrides.md`，只有用户明确要求超过 8 章时才加载。

### 修复

- **阻断弱 coverage term 反向污染正文**：merge、preflight 与最终验证统一拒绝口语指代、语气词和截断片段；工具候选先过滤同类词，来源匹配改为逐字一致，不再因大小写/空白归一化产生假通过。
- **阻断面向门禁的读者补丁**：单章与最终验收拒绝来源没有讨论的“正文证据补丁”“原文痕迹”“覆盖足够长度以满足证据”等文本，并扩展本轮实际出现的口语残留定位。
- **阻断候选目录临时文件**：除 `source-index.json`、`course-manifest.json` 外，最终目录中的批次、计划、图片选择等 `.json` / `.yaml` / `.yml` 一律失败；继续保留辅助脚本与缓存门禁。

### 技术优化

- 新增低信号覆盖词、候选预览证据、批量同理由、整表全删、临时 JSON、证据补丁正文和大小写漂移等故障注入；索引器 15/15、账本工具 30/30、账本预检 20/20、manifest 收口 8/8、最终验证器 58/58 回归通过。

### MiniMax 前向失败记录

- 冻结 v2.9.11 并行完成三轮 MiniMax `minimax-m3` 前向尝试，主模型独立验收为 0/3 可交付。Round 1 形成总览、7 章和 134 项素材，但反复改写来源绑定与覆盖词后最终 10/15、preflight 18 项失败、逐章仅 4/7 通过；Round 2 清理时误删账本并留下空 manifest，最终 9/15；Round 3 正常交付总览与 13 章，但默认上限失败、preflight 仍有来源外覆盖词，且 70/70 候选被一律删除。
- 三轮均有人工语义硬伤：SemVer 旧逻辑回流、来源外强制规则、直接粘贴口语转录、ASR 噪声，以及为证据门禁添加审计补丁。完整报告见外部 `260903 course-generator MiniMax v2.9.11 前向复测/evaluation/v2.9.11/minimax-rounds-1-3.md`。
- v2.9.12 的本地故障注入不能替代新的弱模型生产证据；冻结版 MiniMax 前向运行完成前继续标记 `NOT_VERIFIED`。

### 兼容性说明

- v2.9.12 必须重新执行 `init → plan` 生成 schema 1.8 manifest；旧 schema 1.7 的候选审查没有逐字 `evidence_quote`，不能只修改版本号。source-index schema 仍为 1.4。

## [2.9.11] - 2026-09-03

### 新增

- **候选旧口径逐项审查矩阵**：manifest schema 升级为 `1.7`；每条 `authority_correction_routes` 必须按 source-index 原顺序提交完整 `candidate_block_reviews`，把每个候选块明确判为 `superseded` 或 `retained_current` 并写明理由。
- **弱模型短队列**：`ledger_tool.py init/status` 新增 `authority_candidate_review_queue`，直接汇总每条修正的旧口径、现行口径、候选块 ID、source ref 和短预览，降低弱模型跨文件检索与漏项概率。

### 修复

- **阻断“候选已经召回、模型却漏审后继续写入正文”**：`plan`、preflight 和最终验证器共同检查候选 ID 的数量、顺序、逐项理由及其与隔离列表的一致性；漏审、重复、乱序、`superseded` 未隔离或 `retained_current` 被误隔离都会失败关闭。
- **不把高召回候选整体删除**：候选仍由模型结合预览和必要原文判断；只有明确支持旧口径的块进入 `authority_superseded`，避免为消除矛盾误删案例与操作细节。

### 技术优化

- 索引器 15/15、账本工具 27/27、账本预检 17/17、manifest 收口 8/8、最终验证器 54/54 回归通过；新增 plan 漏审候选、preflight 候选矩阵缺失和最终 manifest 缺少审查字段等故障注入。

### MiniMax 前向失败记录

- 冻结 v2.9.10 的全新 MiniMax-M3[1M] 内部课样本完整生成总览、8 章、82 项素材和 8 张正文图，并正常完成 worker 交付；主模型重跑收口、preflight、八章单章检查和最终领域门禁均通过，最终 15/15。
- 人工语义验收仍一票否决：`COR-002` 已正确注入 SemVer 现行结论，但第 4 章两次恢复“小功能叠 0.0.x / 大功能进 0.x”的旧规则。索引实际已把对应 `BLK-00099` 列为候选，MiniMax 只确认了其他三个块，随后又把漏审块作为两个 include 素材写入正文。
- 本轮另发现测试规格把不存在的旧文件名和过期哈希写入派发；worker 实际读取了同目录带“第1节课 -”前缀的当前文件。因此本轮足以证明 v2.9.10 的候选审查存在结构缺口，但不作为严格同输入复现证据。完整报告见外部 `260902 course-generator MiniMax v2.9.10 前向复测/evaluation/v2.9.10/minimax-round2-internal.md`。
- v2.9.11 的弱模型效果仍为 `NOT_VERIFIED`；必须用正确冻结路径和哈希完成新的 MiniMax 前向生成与主模型语义验收。

### 兼容性说明

- v2.9.11 必须重新执行 `init → plan` 生成 schema 1.7 manifest；旧 schema 1.6 没有候选块逐项审查矩阵，不能手工改版本号冒充迁移。source-index schema 仍为 1.4，可在来源未变化且确定性重建一致时重新绑定。

## [2.9.10] - 2026-09-02

### 新增

- **旧证据块隔离契约**：source-index schema 升级为 `1.4`，每条 `COR-xxx` 增加最多 8 个高召回 `superseded_candidate_block_ids`；manifest schema 升级为 `1.6`，plan 必须逐条声明是否找到旧口径块、确认块 ID 和检索依据。
- **专用替代跳过码**：新增 `authority_superseded`。被当前修订替代的来源块必须全部使用该 code，且不能再进入 include；普通来源块也不能借该 code 逃避素材覆盖。

### 修复

- **阻断“先写正确句、再从旧素材写回相反结论”**：`merge`、账本预检与最终验证器共同检查被替代块的去向，章节提示明确不读取这些块补历史说明、反方观点或例子。
- **保留精华而不整稿作废**：只隔离 plan 明确认定支持旧口径的段落，其余案例、演示、失败修正和操作细节继续进入普通素材账本。

### 技术优化

- 索引器 15/15、账本工具 26/26、账本预检 16/16、manifest 收口 8/8、最终验证器 53/53 回归通过；新增候选定位、plan 状态/块列表一致性、专用跳过码正例和被替代块重新进入 include 的故障注入。

### MiniMax 前向失败记录

- v2.9.9 第一次全新 MiniMax 会话只生成索引与初始化 manifest，连续三个回合返回空响应，记 `PRODUCER_STALLED_EMPTY_RESPONSE`，不评价 Skill 质量。
- v2.9.9 第二次全新 MiniMax 会话完整生成总览、8 章、70 项素材和 6 张正文图；主模型重跑全部机械门禁通过，十条修订句及路由也完整，但 SemVer、Git 防回退、任务/决策是否忽略和运行记录四处以同义句恢复旧结论，记 `DOMAIN_MECHANICAL_PASS + SEMANTIC_CONTRADICTION_FAIL`。
- 该失败直接形成本版“被替代来源块”机制。v2.9.10 的 MiniMax 前向效果在新鲜生成与主模型验收完成前仍为 `NOT_VERIFIED`。
- v2.9.10 第一次全新 MiniMax 会话在模型首响应前连续两次各重试 10 次，均返回 Token Plan 429（2056）；候选目录 0 文件且无 `worker_done`。本次记 `QUOTA_BLOCKED_BEFORE_FIRST_RESPONSE`、`skill_result=null`，不计作 Skill 失败，v2.9.10 继续 `NOT_VERIFIED`。

## [2.9.9] - 2026-09-02

### 新增

- **逐条权威修正契约**：source-index schema 升级为 `1.3`。索引器从声明指定章节的“旧口径 → 修订口径”表确定性生成 `COR-xxx`，保留控制来源行、原口径、修订口径，并只对纯别名行生成 `deprecated_terms`。
- **修正路由与确定性注入**：manifest schema 升级为 `1.5`。`ledger_tool.py plan` 要求每个 COR 按顺序路由到一个已声明 H2；`scaffold` 把权威 `revised_text` 原样写入目标节，弱模型只负责围绕真实素材展开。

### 修复

- **阻断“承认控制文档但仍恢复旧口径”**：单章门禁与最终 `CG-SOURCE-AUTHORITY` 同时检查修订句存在于目标 H2，并在全部读者文件中拒绝 `spec coding`、`RCP/RCap/r cab` 等已确定的纯旧别名。
- **避免粗暴关键词误杀**：只有旧单元格本身完全由一个或多个别名组成时才生成全局禁词；包含解释性正文的旧口径仍允许在课程中用于准确说明历史差异。
- **控制章节名称小幅漂移可解析**：声明写“第 1 节勘误与收束”、控制文档实际标题为“第 1 节勘误与概念归位”时，按相同节号和勘误语义确定性匹配。

### 技术优化

- 索引器 14/14、账本工具 26/26、账本预检 14/14、manifest 收口 8/8、最终验证器 52/52 回归通过；新增修正表编译、章节标题近似匹配、路由缺失、脚手架注入、修订句删除、废弃别名回流和解释性旧口径合法近似反例。

### MiniMax 前向失败记录

- 冻结 v2.9.8 的全新 MiniMax-M3[1M] 内部课样本完成总览、7 章、76 项素材和完整 `worker_done`；主模型重跑自动收口、预检、逐章检查与最终门禁均通过，最终 15/15。
- 人工语义验收仍一票否决：候选恢复 `spec coding`、RCP，把 Monorepo 写成通用物理前提，把符号链接写成跨客户端通解，许可证旧口径与修订口径并存，另有 `dog/task` ASR 噪声。本样本记 `PRODUCER_COMPLETED + DOMAIN_MECHANICAL_PASS + SEMANTIC_HARD_FAIL`。
- 该结果证明 v2.9.8 相比 v2.9.7 已改善权威识别和部分修正，但“总括确认”不足以约束弱模型逐条执行。v2.9.9 的新鲜 MiniMax 前向效果在完成下一轮独立生成与主模型验收前仍为 `NOT_VERIFIED`。

### 兼容性说明

- v2.9.9 必须重建 source-index 并重新执行 `init → plan`；旧 schema 1.2/1.4 没有 COR 清单和章/节路由，不能手工补字段冒充迁移。historical 模式不加载 current 修正规则，继续以总览读者声明保留历史边界。

---

## [2.9.8] - 2026-09-01

### 新增

- **来源权威契约**：`index_sources.py` 默认在 current 模式识别材料开头的编校、非现行与修订声明，精确解析同目录《控制文档》；声明块与控制文档分别标为 `authority` / `control`，不进入普通素材覆盖基线。控制文档缺失或重名时失败关闭，只有用户明确要求历史原貌时才允许 historical。
- **计划阶段强制确认**：source-index schema 升级为 `1.2`，manifest schema 升级为 `1.4`，新增 `source_authority`。`ledger_tool.py plan` 要求 current 模式精确确认控制来源；historical 模式必须提供至少 12 字并原样写入总览的读者提示。
- **权威与过程隔离门禁**：最终验证增至 15 个领域约束，新增 `CG-SOURCE-AUTHORITY`；`CG-AUDIT-SEPARATION` 现在拒绝来源没有讨论的 `source-index.json`、`course-manifest.json` 和 `SRC/BLK/MAT/IMG` 审计 ID。

### 改进

- **降低覆盖词的弱模型负担**：include 素材改为 1—3 个 coverage terms，最低数量为 `ceil(source_block_ids/3)`（最低 1、最高 3）；覆盖词仍须逐字存在于绑定来源，但不再要求同时写入摘要。`merge` 在覆盖词非法时给出确定性原文候选，减少反复试错和为审计硬塞生硬句子。
- **权威处理贯穿写作与验收**：大纲、总览、章节和产物契约统一要求 current 口径覆盖冲突历史表述；historical 总览必须明确非现行边界。机器绑定通过后仍须人工逐项检查控制文档修订是否真正进入正文。

### 技术优化

- 索引器 11/11、账本工具 23/23、账本预检 12/12、manifest 收口 8/8、最终验证器 50/50 回归通过；新增 current 合法绑定/确认错位、historical 提示、控制文档普通“以……为准”不误识别、内部过程术语泄漏、单块单覆盖词和覆盖词无需摘要复述等故障注入。

### MiniMax 前向失败记录

- 冻结 v2.9.7 的新鲜 MiniMax-M3[1M] 内部课候选完成总览、8 章、100 项素材、197/197 个 content block 去向和 7 张正文代表图；主模型重跑自动收口 0 unresolved、preflight PASS、领域门禁 14/14。
- 人工语义验收仍一票否决：候选忽略原始转录稿“不作为现行技术规范、以同目录整合版为准”的编校说明，把声明 skip 并虚称总览已交代，继续传播已修订旧口径；总览还泄漏生成器内部 JSON 文件名。本样本记 `DOMAIN_MECHANICAL_PASS + SEMANTIC_HARD_FAIL`，只证明 v2.9.7 执行流程可完成，不证明成品可交付。
- v2.9.8 尚未获得新鲜 MiniMax 前向结果，弱模型效果与指令稳定性保持 `NOT_VERIFIED`。
- 2026-09-01 使用冻结 v2.9.8 和全新 MiniMax-M3[1M] 会话启动内部课盲测；任务注入成功，但第一次模型响应前连续进入 Token Plan 429（2056）自动重试并耗尽 10 次上限。候选目录 0 文件且无 heartbeat / `worker_done`，本次按 `QUOTA_BLOCKED_BEFORE_FIRST_RESPONSE` 收口，不计为 Skill 结果，不能据此评价 v2.9.8。

### 兼容性说明

- v2.9.8 必须重建 source-index 并升级 manifest；旧的 schema 1.1/1.3 不包含来源权威契约，不能直接通过新验证器。若用户明确要保留历史课堂口径，应重新以 historical 模式索引并在总览披露，不能手工删除权威声明。

---

## [2.9.7] - 2026-09-01

### 新增

- **把弱模型的下一步编译为短命令**：`ledger_tool.py status --next-batch-size` 直接返回下一批唯一的 `content_block_ids`、源图数、正文代表图数、最低选图数与下一动作；`merge` 一次聚合该批全部可独立识别的字段错误，减少逐错重试和剩余 ID 重算。
- **代表图短清单**：新增 `select-images`，模型只提交 `IMG-xxx + target_document_id + reason`；工具按来源顺序同步 `body_action=insert`、目标文档和各文档 `image_ids`，未选择图片自动保留为 `asset_only`，不再让弱模型翻改整份图片 manifest。
- **确定性标题骨架与逐章门禁**：新增 `scaffold`，只创建与冻结计划完全一致的 H1/H2；新增 `check-chapter`，每章写完即检查标题、目标小节覆盖词、图片序列、书稿化命中行和 40% 深度下限，当前章 `PASS` 后才进入下一章。

### 修复

- **通用覆盖词提前失败**：`merge` 与 `preflight_ledger.py` 同时拒绝全部由 `AI`、`Agent`、`Skill`、`内容`等通用词组成的 coverage terms，并按来源块数量检查词数，修复“账本预检通过、最终门禁才失败”的阶段不一致。
- **逐字稿残留精确定位**：`CG-BOOKLIKE-TONE` 扩展到模糊讲者指代、课程/演示转播框架和明确口语赘词，失败报告给出命中总数与行号，不再只报文件名。章节与总览提示同步要求重组整段，不接受机械删词或在成品中保留修订说明。
- **大纲图片流程减负**：全量图片身份、哈希与顺序继续由工具保真；大纲只保留代表图选择视图，不再要求弱模型复制上百项图片资产表。
- **混合重试不再跳号或污染后项**：`merge` 只在素材真正写入时分配连续 `MAT-xxx`；失败素材先完整检查冲突，确认合法后才更新批内状态，避免“已有重试项 + 新素材”产生编号空洞，也避免失败的多来源素材让后续合法项误报冲突。
- **执行越序失败关闭**：`status`、`select-images`、`scaffold` 和 `check-chapter` 统一校验绑定索引哈希；账本未覆盖全部 content block 时禁止选图或生成章节，任一计划 H2 没有 include 素材时禁止搭脚手架，重复 H1 也会在单章门禁失败。

### 技术优化

- 索引器 7/7、账本工具 21/21、账本预检 12/12、manifest 收口 8/8、最终验证器 45/45 回归通过。新增批次聚合错误、全通用覆盖词、混合重试连续编号、失败项状态隔离、索引漂移、执行越序、必选代表图缺失、空 H2、重复 H1、代表图同步、单章覆盖/图片/深度/文风和课程框架/口语赘词故障注入。
- manifest schema 仍为 `1.3`，source-index schema 仍为 `1.1`；本次增加的是执行工具与失败语义，不要求已经完成 v2.9.6 迁移的课程再次改 schema。

### MiniMax 前向失败记录

- 冻结 v2.9.6 的全新 MiniMax-M3[1M] 无锡样本生成了总览、8 章、203/203 个 content block 去向和 8 张代表图，自动收口为 0 unresolved；但主模型重跑冻结门禁仍为 13/14，`CG-BOOKLIKE-TONE` 在 CH05—CH08 命中课程框架与逐字稿口语。更严格的 v2.9.7 门禁在 CH02—CH08 定位出“后续演示/演示中/讲者/这样的一个/比如说/就是说/什么什么”等残留。
- MiniMax 至少两次在未实际重跑或未通过验收时停止；主模型退回精确行号后，worker 再次触发 Token Plan 429（2056），未形成 `worker_done`，候选按失败保留。该轨迹支持本次“把机械流程编译进工具”的方向，但 **不构成 v2.9.7 前向通过证据**；新鲜 MiniMax 有效样本仍为 `NOT_VERIFIED`。
- 2026-09-01 另以冻结 v2.9.7、内部课转录稿和全新 MiniMax-M3[1M] 会话启动盲测；模型界面与 provider 路由均确认无误，但第一次响应前即连续 10 次返回 429（2056）。候选目录为 0 文件且无 `worker_done`，本次登记为 `QUOTA_BLOCKED`、不计有效样本，不能据此评价 v2.9.7 的生成效果。

---

## [2.9.6] - 2026-08-31

### 新增

- **弱模型增量账本工具**：新增 `ledger_tool.py`，提供 `init`、`plan`、`merge`、`status` 四个确定性入口。来源、图片、索引哈希、`MAT-xxx` 连续编号和 `source_refs` 由工具生成；弱模型只在 20—40 个 content block 的有界批次里判断素材语义、coverage terms 与目标章/节，完全相同的批次重试会幂等跳过。
- **章/节级证据契约**：manifest schema 升级为 `1.3`。章节必须声明最终 `section_headings`，每个 include 素材必须绑定 `target_section_heading`；finalizer 只从目标 H2 范围抽取证据，preflight 与最终验证器要求每个计划 H2 至少承载一个 include 素材。

### 修复

- **阻断“机器 14/14、正文仍来源外生长”**：MiniMax 无锡候选通过 v2.9.5 全部门禁，但人工发现“Skill 库与团队知识管理”等无精确素材 H2，并补写版本控制、依赖管理、案件归档系统打通等来源没有的内容。v2.9.6 要求实际 H2 与冻结计划文字、数量和顺序完全一致，新增无素材 H2 故障注入。
- **阻断候选目录污染**：最终门禁拒绝 `.py`、`.sh`、`.js`、`.ts`、`.command` 辅助脚本和 `.course-work`、`__pycache__` 缓存；批次 JSON 与生成辅助文件必须留在课程目录之外。
- **账本预检重绑原文**：`preflight_ledger.py` 支持 `--source-root`，确定性重建来源块并检查每个 coverage term 同时存在于素材摘要与绑定原文，减少弱模型把抽象词写回摘要的逃逸口。

### 技术优化

- 新增账本工具 9 类正常/重试/冲突/章节越界回归，默认八章在写正文前即失败关闭；账本预检 11/11、manifest 收口 8/8、索引器 7/7 继续通过；最终验证器故障注入由 40 类增至 43 类，新增同章跨节借证据、无素材 H2 与候选辅助脚本反例。
- 静态安全扫描无 high/critical，Harness 失效模式审计无 finding；指令稳定性门禁因 v2.9.6 尚无三轮真实弱模型有效产物，继续返回 `INSTRUCTION_STABILITY_NOT_VERIFIED`。
- 官方 Skill Creator quick validator 仍因项目/ClawHub 规范要求的顶层 `version`、`author`、`homepage` 字段返回不兼容；本次不删除这些项目必需元数据，记录为已知兼容性失败。

### 失败记录

- 三个全新 `MiniMax-M3[1M]` 会话用冻结 v2.9.5 分别处理智合、无锡和内部课，额度从 100% 消耗至 Token Plan 429，0/3 完成结构化 `worker_done`。候选机器结果为无锡 14/14、内部课 10/14、智合 8/14；无锡再因来源外 H2 被人工判定失败，故产物级有效率仍为 0/3。修订后的 v2.9.6 未获得新鲜 MiniMax 前向复测，不扩大为弱模型效果已提升。

### 兼容性说明

- manifest schema 从 `1.2` 升级为 `1.3`。旧课程需为每章补充冻结的 `section_headings`，并把每个 include 素材绑定到一个 `target_section_heading` 后再复查；不能用自动迁移替代人工判断素材应进入哪个小节。

---

## [2.9.5] - 2026-08-30

### 修复

- **把弱模型的机械审计工作移交给确定性脚本**：新增 `finalize_manifest.py`。账本阶段从 `source-index.json` 自动同步素材 `source_refs` 与索引 SHA；终稿阶段只从最终 Markdown 中选择满足现有契约的 1—3 段正文证据，并按正文实际图片同步文档 `image_ids`、`body_action` 与目标文档。脚本不写读者正文、不发明缺失事实；无法满足覆盖词、长度或唯一性时返回精确 `unresolved` 并失败关闭。
- **阻断泛化跳过吞掉课程精华**：`pure_repeat` 与 `no_course_value` 合计只能覆盖 `max(1200 字, 全部 content block 字符数的 5%)`。超过预算在长篇生成前由账本预检阻断，最终验证器同样以 `CG-SOURCE-BLOCK-COVERAGE` 失败；会务、设备、寒暄与平台派生内容仍按专用 skip code 处理。
- **减少弱模型配额浪费**：生成顺序调整为“索引字段自动收口 → 账本预检 → 完整正文 → 证据/图片自动收口 → 最终验证”。缺失正文只修报告指定的素材，不再让模型反复人工维护 68—102 组证据和整套图片映射。

### 技术优化

- 新增 manifest 自动收口 8 类回归，覆盖 dry-run、账本阶段隔离、source refs、证据唯一性、图片映射、幂等性、原子写入权限保留、未解决项不编造及读者文件不可读时不破坏原映射。
- 素材账本预检由 7 类增至 11 类，新增 source refs 确定性映射、content block 漏登记、泛化跳过超预算反例和大体量 `device` 合法近似正例；最终验证器故障注入由 38 类增至 40 类。
- 对三份 MiniMax v2.9.4 半成品做只改临时副本的历史回放：自动修复智合 4、无锡 9、内部课 15 组正文证据，并同步图片或 source refs；剩余问题分别精确收敛为 25、3、1 个 unresolved。智合另被新增预算识别出 8842 字泛化跳过，说明脚本能减少机械修补，同时不会把缺失精华伪装成通过。

### 失败记录

- v2.9.4 的三个全新 MiniMax-M3[1M] worker 均完成来源索引、账本和 7—8 章读者正文，但在最终收口阶段触发 Token Plan 429（2056）。主模型按冻结 v2.9.4 验证器复核为 10/14、13/14、12/14，全部按失败处理；第二、三波因停止条件未再消耗额度。

### 前向复测记录

- 额度恢复后，使用三个彼此独立的 MiniMax-M3 新会话分别处理智合、无锡律协和内部 Skill 工程化课程；运行前冻结 v2.9.5 Skill、脚本与验收协议，主模型不参与正文生成。供应商额度由 99% 降至 0%，三个 worker 都未在限额前完成 Orca `worker_done` 生命周期，因此任务交付按 0/3 失败登记，不以半成品冒充正常交付。
- 智合候选形成完整产物：主模型运行冻结自动收口为幂等 PASS（0 个 unresolved、未改文件），账本预检 PASS，最终 14/14 门禁通过，逐篇语义评分 92/100。该样本证明 v2.9.5 能让 MiniMax 单次写出真实可用课程，而不是只把强模型修补结果算作成功。
- 内部课候选留下自建辅助脚本且原生仅 8/14；冻结自动收口虽修复 19 组证据，仍因 33 组 coverage term 无法从正文兑现而失败关闭，主模型复核为 10/14。无锡候选只完成来源索引。第二、三波依停止条件取消，故弱模型跨课程稳定性仍为 `NOT_VERIFIED`。
- 追加精确模型 `glm-5.3-flash[1M]` 自适应前向复测：先跑内部课，只有完整通过且额度不低于 35% 才扩展另外两课。单个新会话在约 34 分钟内把额度从 62% 消耗到 15% 停止线，未完成 `worker_done`；第二阶段未启动，worker 生命周期与有效产物均为 0/1。
- GLM-Flash 候选的 105 项素材、197/197 个 content block 去向和 78 张源图通过冻结账本自动收口与预检，但没有生成任何读者正文，还自建 381 行 `_build_manifest.py`。主模型最终复核为 10/14，确认账本契约有效、最终门禁能拒绝空正文，同时暴露索引后无界规划、整份 manifest 机械构造和候选目录污染会在正文开始前耗尽额度；课程正文质量与模型稳定性继续标记 `NOT_VERIFIED`。

### 兼容性说明

- manifest schema 仍为 `1.2`，source-index schema 仍为 `1.1`。旧课程可直接运行自动收口和最终验证，但大量使用 `pure_repeat` / `no_course_value` 的成品可能新增失败，需要人工重新判断被跳过内容，不能用迁移脚本自动改成 include。

---

## [2.9.4] - 2026-08-26

### 修复

- **弱模型素材 ID 提前失败关闭**：include 与 skip 统一使用连续 `MAT-xxx` 命名空间，明确 `skip` 只是 disposition、禁止另造 `SKIP-*`。新增 `preflight_ledger.py`，在长篇正文生成前检查 ID 连续性、include/skip 字段、coverage terms 和来源块去向，避免把可在账本阶段发现的错误拖到最终验收。
- **去讲者口吻不再抹去证据边界**：事实性步骤仍直接书面化；个人判断、产品自评、事故转述、时间预测和绝对化效果保留“课程判断/案例描述/本次运行”限定，避免弱模型把来源中的口头主张改写成课程的无条件客观事实。
- **失败停止条件**：账本预检失败只修报告字段，不重抽原文、不改弱 coverage terms；配额、网络或平台异常时保留半成品并报告失败，不从头重跑整门课程。

### 技术优化

- 新增 7 类素材账本预检回归，覆盖合法 include/skip、`SKIP-*` 平行编号、ID 断档、重复 ID、skip 目标章节、include 覆盖词和同一来源块 include/skip 冲突。
- 最终验证器同步强制 `materials.id` 从 `MAT-001` 按数组长度连续分配，故障注入由 37 类增至 38 类；即使弱模型跳过生成前预检，也不能让断档或越界编号进入成品。
- GLM-5.3[1M] 第一轮三门真实课程均生成完整产物并由主模型独立重跑 14/14 领域门禁；第二轮因供应商额度耗尽 0/3 有效，其中一份完整候选暴露 `SKIP-*`、证据过短和正文深度不足，均按失败处理。三份有效产物人工语义评分为 93、93、90；由于没有形成同输入三次有效运行，稳定性继续标记 `NOT_VERIFIED`。

### 兼容性说明

- manifest schema 仍为 `1.2`，source-index schema 仍为 `1.1`；v2.9.3 课程可直接用最终验证器复查。`preflight_ledger.py` 是新增的生成前 fail-fast 步骤，不改变已完成课程的数据格式。

---

## [2.9.3] - 2026-08-26

### 修复

- **阻断 coverage term 事后造词**：新增 `CG-CLAIM-FIDELITY`，要求每个预承诺词真实存在于素材绑定的原始来源块；正文自行补出的“英文缩写（英文释义）”如果无法在原始来源定位，同样失败。生成验收现在必须提供可重绑定的 `--source-root`。
- **解除单段证据的弱模型循环**：manifest Schema 升级为 `1.2`，将 `reader_evidence.quote` 改为 1—3 段 `reader_evidence.quotes`；覆盖词和最低证据长度在合并文本上核验，避免为把所有词挤进一段而专写审计文字。
- **隔离平台二次摘要**：source-index Schema 升级为 `1.1`；转录包同时存在原始“转录内容”和后续平台摘要标记时，将关键词、议程摘要、重点内容、Q&A、PPT 章节标题标为 `derived`，不再进入 content 覆盖基线或独立提供事实。
- **阻断机械拆章和模板直出**：默认章节上限为 8，只有用户明确要求时才可用 `--max-chapters` 提高；读者文件名残留方括号占位符、`TBD`、`TODO` 或跨平台非法字符时验收失败。开班、签到、领导致辞、设备/安装准备和讲者履历默认合并或跳过。
- **补齐同行多图索引**：同一行包含多张 Markdown 图片时逐张生成 image block，继续满足源图片全登记契约。

### 技术优化

- 索引器回归由 5 类增至 7 类；验收器由 32 类增至 37 类，新增平台附录降级、同行多图、来源外覆盖词、来源外缩写释义、默认超八章、模板文件名反例和用户明确提高上限的合法正例。
- 在两份真实转录稿上重建索引：无锡由 253 个 content block 调整为 203 content + 77 derived，131 张图片保持完整；内部课由 251 个 content block 调整为 188 content + 83 derived，78 张图片保持完整。

### 失败记录

- v2.9.2 的两个全新 MiniMax-M3[1M] 前向任务均因 Token Plan 达到 2056 上限而返回 429，按冻结协议记为 FAILED、不评分。额度耗尽前已观察到单摘录证据循环、来源外 coverage term、十二章机械拆分、模板方括号直出和 `RCP` 全称补写；v2.9.3 据此修正，但其 MiniMax 前向效果保持 `NOT_VERIFIED`。

### 兼容性说明

- v2.9.2 产物不能直接通过 v2.9.3：需要重建 `source-index.json`、把 manifest 升级为 `1.2`，并将 `reader_evidence.quote` 迁移为 `reader_evidence.quotes`。

---

## [2.9.2] - 2026-08-26

### 修复

- **阻断源图片账本漏项**：MiniMax 跨课程 A/B 中，v2.9.1 内部课候选只登记 78 张源图中的 13 张仍通过门禁。新增 `CG-IMAGE-SOURCE-COVERAGE`，从 source index 复算全部 image block，要求 manifest 按原顺序、原 source ref 与原 Markdown 哈希逐项登记。
- **阻断图片密集课程零配图**：无锡候选将 131 张源图全部降级为 `asset_only` 仍通过门禁。新增 `CG-IMAGE-SELECTION`：来源图达到 12 张时，整套课程至少选择 `min(读者文档数, ceil(源图片数 / 20))` 张代表图；继续使用现有密度上限防止全量搬图。
- **收紧弱模型来源外补写**：把详尽度明确限定为“展开来源已经给出的要素”，禁止补全缩写、技术参数、产品能力、角色分工、行业案例、商业模式和时间预测，并要求保留可能性、举例和设想的原始语气。
- **移除诱导填充的默认模块组合**：不再要求每章至少两个预设模块；`最佳实践`、`未来展望`、`延伸阅读`等结构只有来源确有对应内容时才可使用。

### 技术优化

- 验收约束从 11 项增至 13 项；故障注入由 29 类增至 32 类，新增源图漏登记、图片密集来源零插图两个失败样例和少量低价值图片零插图的合法近似正例。
- 完成两个新课程、四个独立 MiniMax-M3[1M] 会话的 v2.8.1 / v2.9.1 A/B。主模型独立验收结论为“覆盖与深度机制有效，但总质量尚未跨样本稳定提升”；详细证据见外部测试目录，未用生成 worker 自评替代验收。

### 兼容性说明

- manifest Schema 仍为 `1.1`；v2.9.0/v2.9.1 产物可以升级图片账本后复查，但源图登记不完整或图片密集来源零正文图的课程会新增失败项。
- v2.9.2 的 MiniMax 前向回归完成前保持 `NOT_VERIFIED`，不以静态规则修改声称弱模型效果已经提升。

---

## [2.9.1] - 2026-08-26

### 修复

- **修复来源块全覆盖但正文仍缩水**：同一真实长转录稿由两个独立 MiniMax-M3 worker 分别执行 v2.8.1 / v2.9.0 后，v2.9.0 虽通过 9 项门禁，读者正文却从约 3.5 万可见字符降至约 1.66 万；匿名 MiniMax 盲评为 86:72，退化集中在高价值素材覆盖、操作细节和独立可读性。根因是模型把来源块登记与最低证据长度误当成写作目标。
- **限制素材过度合并**：include 素材最多绑定 6 个相邻来源块；coverage terms 数量随来源块数量增加，证据最低长度也按素材类型与来源块数量递增。明确 `BLK` 只是盘点单位，`MAT` 必须按读者可复用的信息单元拆分，并固定“先写完整正文、后回填证据”的生产顺序。
- **阻断摘要化交付**：新增 `CG-READER-DEPTH`，按最终章节可见文字与纳入来源字符量复算最低深度（单章 40%、全局 55%）。该门禁只识别明显缩水，不替代人工语义复核。
- **阻断正文图片过载**：新增 `CG-IMAGE-DENSITY`，每份读者文档与整套课程按 `max(3, ceil(可见文字数 / 500))` 约束正文图片上限；连续截图保留代表图，其余转为 `asset_only`，且不允许同步压缩正文。

### 技术优化

- 验收器故障注入由 26 类扩展为 29 类，新增长来源短正文、单素材吞并过多来源块和短正文堆图三个历史回归；继续保留人工复核边界，不把字数和图片密度表述为语义质量证明。
- 完成备用弱模型同源 A/B：两个全新 DeepSeek-v4-flash 会话分别执行 v2.8.1 / v2.9.1，均通过各自门禁；v2.9.1 可见正文较旧版增加约 18%，并由第三个全新 GLM-5.3 匿名评审以 91:86 胜出。MiniMax v2.9.1 前向轮因 Token Plan 配额耗尽只形成不完整样本，不计入胜负；因此仍不声称跨任务稳定性已验证。

### 兼容性说明

- manifest Schema 仍为 `1.1`；v2.9.0 产物可直接用 v2.9.1 验收器复查，但此前通过、实际正文过薄或插图过密的课程可能新增失败项，需要补正文或下调正文图片后再交付。

---

## [2.9.0] - 2026-08-25

### 新增

- **确定性来源索引**：新增 `scripts/index_sources.py` 与 `source-index.json` Schema，按稳定文件/行号顺序生成 `SRC-xxx` / `BLK-xxxxx`，记录原始文件与块级 SHA-256；弱模型不再自行决定“哪些段落存在”。
- **来源块覆盖门禁**：每个 `kind=content` 的来源块必须进入素材，或使用受控 `skip_code` 和具体理由跳过；未知块、非 content 块、未分配块或 include/skip 冲突块均验收失败。
- **预承诺正文证据**：include 素材在大纲阶段从摘要预先选定 2—5 个具体 `coverage_terms`，生成后以目标章节的真实连续摘录兑现；禁止用全是通用词的摘录或事后改弱覆盖词伪造完成。

### 改进

- **约束验收、保留写作自由**：硬契约只限定来源存在性、素材去向、关键事实覆盖和图片对应；章节数、主题组织、叙事顺序和措辞仍由素材与生成者决定，避免课程退化为固定模板。
- **原始来源重绑定**：`verify.sh` 支持 `--source-root`，验收时重新枚举完整输入范围并重算原始来源 SHA-256，避免漏掉整份来源文件，或用过期索引验收已变更的转录稿。

### 技术优化

- 新增索引器 5 类回归样例，覆盖块分类、同行发言人标记后实质内容保留、ID/哈希稳定性、目录排序/隐藏文件排除和输出目录反向污染；验收器故障注入扩展为 26 类正反例，包含整份来源文件漏失，全部 fail-closed。
- 用一份约 18.7 万字节、135 张幻灯片图的真实转录稿完成基线轮与修订轮前向测试；修订轮对 376 个内容块、74 个 include 素材和 55 张正文图片完成确定性验收。

### 兼容性说明

- manifest Schema 从 `1.0` 升级为 `1.1`，v2.9 必须同时提供 `source-index.json`、细粒度 `source_block_ids`、预承诺 `coverage_terms` 和 `reader_evidence`；v2.8 课程需升级 manifest 后再验收。

---

## [2.8.1] - 2026-08-25

### 修正

- ClawHub 发布元数据：v2.8.0 首次发布漏传 `--name`，display name 退化为临时目录名 `Clawhub Publish Course Generator`。bump 版本带 `--name "课程生成器"` 重新发布，修正为正确显示名。本地 skill 本身无功能改动。

## [2.8.0] - 2026-08-24

### 新增

- **`course-manifest.json` 产物契约**：新增版本化 JSON Schema 与字段说明，以 `SRC-xxx`、`MAT-xxx`、`IMG-xxx` 双向绑定来源、素材、章节和图片；`00 + 章节` 作为读者成品，manifest 与可选 `98/99` 文件作为审计产物。
- **标准库领域验证器**：新增 `scripts/verify_course.py`，由 `verify.sh` 兼容入口调用；精确检查 manifest、总览/章节完整性、未声明章节、素材映射、图片集合/目标/顺序、书稿化转播句式和审计元数据分离，最后输出带产物 SHA-256 的 JSON。
- **故障注入回归套件**：新增 `scripts/verify_selftest.py`，覆盖合法近似正例、带空格文件名、缺/空 manifest、来源路径穿越、空章节、缺图、乱序、未声明图片、素材断链、转播句式、可见 source ref、仅资产图误插和未声明章节共 13 类样例。

### 改进

- **长材料改为索引化两遍流程**：先建立稳定来源/段落/时间索引与素材账本，再合并全局大纲；逐章只读取对应 source refs 与邻接上下文，不再要求一次性读取全部文件。
- **图片顺序契约消除冲突**：来源全局顺序仍用 IMG 编号记录；主题重组后的正文顺序以 manifest 的目标文档 `image_ids` 为准，验证器比较原始 Markdown 的精确序列，不再同时要求全章 IMG 数字严格递增。
- **生成模式成为默认主入口**：归档和提取保留为显式后续动作；生成不再依赖归档配置、不自动归档，归档默认复制且不静默覆盖或移动。
- **触发边界收窄**：明确仅 ASR 纠错交给 `transcription-corrector`、讲课表现复盘交给 `lecture-review`、多篇材料成书交给 `article2book`。
- **书稿化规则去误伤**：把“讲者/讲师/主讲人归零”收窄为禁止其作为动作发出者，允许“讲师资格”等真实主题词；把“内容一字不可少”改为高价值素材不得无理由净丢失，允许合并重复和赘词。

### 修复

- 修复 v2.7.5 验收器只比较资产总数与正文非零、会放过部分必插图片缺失的问题。
- 修复 shell 以空格拆分章节文件名，导致带空格空章节绕过检查的问题。
- 修复旧大纲“主题重组允许小节内递增”与章节自检“全章必须严格递增”的直接冲突。
- 修复公开路径示例包含个人课程体系文件名与业务目录结构的问题。

### 技术优化

- 验收脚本仅依赖 Python 标准库；验证器异常、目录错误、manifest 缺失或空检查范围均 fail-closed。
- 课程工具许可证按项目通用工具规范统一为 MIT，并同步 frontmatter、`LICENSE.txt` 与 README。

### 兼容性说明

- v2.8 验收器要求课程目录存在 `course-manifest.json`；旧课程不会被启发式规则伪判通过，需要先补 manifest 后再验收。

---

## [2.7.5] - 2026-08-17

### 新增

- **验收脚本 scripts/verify.sh**：把 Step 9 验收中"可 grep 核查"的项从"Agent 自觉 grep"变成"脚本客观执行"。脚本自动检查六项：完整性（总览+章节+非空）、讲者/讲师/主讲人归零、口语赘词归零、来源痕迹框架词归零、图片漏插与编号递增、大纲英文专名素材覆盖。退出码 0=通过、1=存在失败项。实测：对华商 2.7.3 缺陷产物运行，正确报出"缺 00 总览 + 通篇漏插 244 图 + Meta/cron 专名缺失"。

### 改进

- **生成/验收分离 + 脚本强制验收**：Step 9 明确"验收分工原则"——可 grep 核查的项（配图、素材覆盖、术语、完整性、讲者归零、口语赘词）交给脚本客观执行；语义判断（跨章一致性、溯源）由主控方人工完成。生成阶段不必对可核查项逐条死守，脚本在验收时兜底。回应"规则多导致注意力分散"的根因：生成减负、验收兜底。
- **脚本纳入强制验收步骤**：生成模式流程行加入"运行 verify.sh 强制验收"节点；Step 9 标题标注"强制，不可跳过"；明确脚本退出码非 0 即验收失败、任务不得标记为完成，跳过脚本或脚本未通过即交付属于未完成验收。

### 文档完善

- 更新 `SKILL.md`（Step 9 改为运行脚本 + 人工语义检查，参考文档加 verify.sh）。
- 新增 `scripts/verify.sh`（推翻 DEC-003 的"不需要 scripts/ 目录"决定，见 DEC-028）。

---

## [2.7.4] - 2026-08-17

### 修复

- **验收缺"漏插检查"与"素材覆盖抽查"**：华商 2.7.3 实测暴露两类执行失败——7 章正文一张配图都没插（注意力被"讲者归零"吸走，漏执行 Step 7 配图插入），以及 07 章丢失 Obsidian 推荐、最强模型创建 Skill 的分层用法、微信"搜索传票"演示等原文有内容。根因是 Step 9 验收的"图片保真"只查"已插入的图是否一致/乱序"，不查"该插入的图是否漏插"；素材覆盖只靠"生成过程自检、不落盘"，无验收兜底。规则补丁：
  - Step 9 验收新增**漏插检查**——grep 比对大纲标注"插入正文"的 IMG 编号是否真的出现在对应章节正文，漏插必须补上；明确"验收不能假设图片已插入，先核对'该插的插了没'，再核对'插的对不对'"
  - Step 9 验收新增**素材覆盖抽查**——从大纲每章高价值素材清单抽关键名词 grep 比对正文，未出现的回原文确认是否应补
  - chapter_prompt 硬性自检新增"配图漏插"逐张核对，溯源抽查的"核对正文配图"改为"先查漏插、再查乱序"

### 文档完善

- 更新 `SKILL.md`、`references/chapter_prompt.md`。

---

## [2.7.3] - 2026-08-17

### 技术优化

- **结构精简与解耦**：用户指出 skill 规则过多导致执行时注意力分散（华商"讲者"77 次 vs 云南 1 次的不稳定即是证据），且存在大量跨文件重复。精简三处：
  - SKILL.md 从 339 行瘦身为约 175 行——删除与 references 重复的 11 模块表格、篇幅量纲、凝练细则、图片筛选细节，核心原则从 11 条压成 5 条一句话，新增"规则优先级"表解决规则冲突裁决
  - chapter_prompt.md 从 300 行精简为 158 行——删除 11 个模块模板示范代码块（样板文字），"自然表达"10 个子点合并为 4 个
  - 单一事实源：每条规则只在执行点保留权威定义（图片资产/术语→outline_prompt，去来源痕迹/金句/篇幅→chapter_prompt），其他文件改一句引用
- 核心生成链路（SKILL + outline + overview + chapter）从 866 行降至约 560 行（-35%）

### 文档完善

- 更新 `SKILL.md`、`references/chapter_prompt.md`。

---

## [2.7.2] - 2026-08-17

### 修复

- **"讲者"第三方语气残留**：v2.7.0 的"流程实录 + 金句引用"落地时，生成代理把禁词"主讲人"替换成"讲者"继续用，句式不变（"讲者强调/推荐/警告/坦言"），华商 v2.7 重制版"讲者"出现 77 次（旧版仅 29 次），破坏"总结出的精华课程"定位。规则补丁：通篇禁用"讲者/讲师/主讲人"作主语或动作发出者并给转化示范；流程实录的指令原话改用"下达指令/输入"无主语方式引出；自检新增"讲者清零 + 口语赘词清零"硬性检查。
- **去来源痕迹连坐删除工具细节**：华商 v2.7 重制版第一章"通义听悟"从旧版一整节（章节速览、PPT 智能截取、按发言人总结、选中文字判断双层 PDF）缩成 1 次带过，"章节速览/PPT 智能截取/选中文字/PPTX/并非所有 PDF 都要 OCR"全部归零。根因是去来源痕迹把承载在框架词里的工具功能细节、判断技巧、取舍判断和个人工作习惯连坐删除。规则补丁：明确"工具使用细节与判断技巧必须保留"；大纲新增该素材类别；章节边界控制新增"分流不丢失"。
- **篇幅量纲封顶暗示压缩**：原"实践章 2500-3500 字"被当作上限压制了素材展开。改为"下限，不封顶"，以"每个素材点完整展开、原文区间全部覆盖"为准。

### 文档完善

- 更新 `SKILL.md`、`references/chapter_prompt.md`、`references/outline_prompt.md`、`references/overview_prompt.md`。

---

## [2.7.1] - 2026-08-17

### 修复

- **多篇分段转录稿的图片全局顺序**：某 4 篇分段转录课程（约 230 张图）实测暴露——幻灯片编号是各篇独立的，生成代理误用幻灯片号判断全局顺序导致跨章错配与漏插。规则补丁：多篇合并处理时资产表必须跨篇连续重编 IMG-xxx，所有顺序判断只认 IMG 编号（URL 时间戳可作校验，实测 217 张中 216 张一致）。
- **主题重组的图片顺序例外**：跨篇按主题重组的章节，小节顺序与原时间线不一致时，图片按内容就近匹配小节、小节内保持 IMG 递增，不强行凑全局递增把图插到不相关小节；大纲配图清单标注"按正文出现顺序"。
- **归档命名纪律**：三门课重制时误将另一门课的课名套用到全部课程。新增 Step 8 命名纪律：课程名称取自该项目自己的对外大纲/报价方案；归档日期用培训实际日期而非生成日期；主办方写法与项目沟通文件一致。

### 应用

- 分段课 v2.7 重制版完成图片验收修正：31 张正文配图归位、4 张运行模式图从错配章节归位到正确小节，大纲配图清单与汇总表同步为验收终态。
- 三门课程的知识库目录与内部课名已对齐各自项目的对外大纲命名（具体课程与命名细节记录在本地 DECISIONS，不入公开仓库）。

---

## [2.7.0] - 2026-08-17

### 改进

- **金句引用从"原话保留"转为"凝练转写"**：用户反馈逐字引用口语（如"我做出来的只是一个最大公约数，你们可以做最贴合自己工作场景的"）在书稿中过于口语化，但这类判断本身是画龙点睛之笔。引用规则改为：保留讲者的判断、比喻与表达骨架，压缩口语连接词、语气词、重复和"这个/一个"式赘词，改写为凝练书面金句；凝练只动语言、不动判断，不得添加原文没有的观点。长段踩坑叙事不入引用，写成正文。
- **正文书面化**：口语表达（"薅羊毛""改岔""造出来""把它接下来"等）转写为书面表达；生动比喻与概念命名（"乐高积木""最大公约数"）可保留但用书面语法承载；"大家""我们听课"等课堂称呼改为面向读者的一般陈述。
- 对 Agent 下达的操作指令原话（如"请把我的整个流程沉淀成一个 Skill"）仍按原样保留在流程实录中——指令本身就是可复用的操作素材，仅轻度清理明显的转写赘词。

### 应用

- 260815 课程全稿（00-08）完成书面化润色：22 处金句引用凝练、正文口语残留清除；99 大纲金句汇总表同步更新为凝练版（原话可按出处时间回溯）。

---

## [2.6.0] - 2026-08-16

### 改进

- **新增「流程实录」展开形态**：连续三步以上的操作/演示链不再拆散为分布各节的压缩句，写成完整连续实录——按实际发生顺序推进，每步含界面动作、对 Agent 的指令原话（行内引号保留）、Agent 反应、产出结果与途中插曲。触发背景：v2.5.1 版用户反馈章节仍偏短，实证缺口在"过程颗粒度"——如自建 Skill 章的创建实录（发起沉淀指令→Agent 给出技能结构与多余步骤→人工确认→技能落位→复测→纠错→复现）被压成七八个散句，流程的连续叙事丢失。
- **去来源痕迹规则澄清**：明确"删的是'现场/演示'框架词，不是操作过程本身"——在哪个界面点了什么、对 Agent 说了什么、Agent 如何回应是正文素材。此前的表述导致生成时操作过程被连坐删除。
- **篇幅量纲**：实践演示类核心章节正文一般 2500-3500 中文字，概念类章节 1500-2500 字；每个操作/案例素材点平均展开 150-300 字；低于量纲先回原文补过程细节再收笔。给"更长"一个可执行的基准，防止回落到紧凑文风。

### 文档完善

- 更新 `SKILL.md`、`references/chapter_prompt.md`。

---

## [2.5.1] - 2026-08-16

### 修复

- **图片顺序违规高发**：v2.5.0 全量重跑实测中，图片被按语义就近放置而乱序是出现率最高的违规（多个章节违反 IMG 原序）。自检小节新增图片编号递增核对，并明确裁决规则：叙事顺序与图片原始顺序冲突时，以图片原始顺序为准调整小节结构。

### 改进

- **新增 Step 9 验收检查**：保存完成后对实际产物执行验收（完整性/图片保真与顺序/跨章一致性/术语抽查/溯源抽查），明确"验收方式是读产物文件本身，不信任生成过程的自检结论"。实测依据：生成代理自检自报"无问题"的章节，逐句核对后发现图片乱序、素材缺失与演绎——自报不可作为验收依据。

---

## [2.5.0] - 2026-08-16

### 修复

- **加厚引入的演绎风险**：v2.4.0 分类型展开增大了补细节冲动，实测发现成稿混入原文没有的内容（如"中途断了还得自己核对缺了哪份"）和讲者现场行为被推广为通用建议（如"外发前应做遮挡"）。新增"展开≠演绎"纪律：成稿中的数字、动作序列、后果和建议必须能在原文定位出处；讲者行为如实描述但不推广为建议；推断须用明显的推论句式；宁可少一句流畅的展开，不可多一句无法溯源的细节。

### 改进

- **逐段扫描兜底**：大纲素材识别从整体印象改为逐段扫描——每个实质发言段落归入章节素材或跳过类别（会务/设备/寒暄/纯重复/确认性插话），闭环大纲阶段的无声丢失；讲者被幻灯片切换打断的截断句依据上下文归入素材，无法判断的按跳过处理。
- **原文区间标注**：大纲为每章标注素材对应的原文时间范围，穿插分流内容注明去向。
- **双基准自检**：章节自检从"对照大纲清单"升级为"大纲清单 + 原文区间回扫"——逐段确认覆盖或属跳过类别；成稿数字（页数、时长、条号、金额）与专名回原文核对，转录误转按上下文校正；溯源抽查发现无法定位出处的细节直接删除。
- **金句与可迁移表达汇总**：大纲新增金句汇总（原文 | 出处时间 | 归属章节 | 用途），随 99 文件保存，供正文引用和后续写书、公众号文章复用。

### 文档完善

- 更新 `SKILL.md`、`references/outline_prompt.md`、`references/chapter_prompt.md`。
- 新增 `DECISIONS.md` [DEC-021]。

---

## [2.4.0] - 2026-08-16

### 修复

- **章节正文单薄**：修复 v2.3.x 生成的章节相对转录稿原文过薄的问题。实证定位：段落长度硬约束（160-240 字）与素材点数量相除后，每个高价值素材平均只分到约 60 字、恰好一句话；"少压缩、多保留"因无量纲支撑被排版约束压平。

### 改进

- **分类型展开深度**：高价值素材按类型决定展开深度——案例类（背景/做法/结果/启发至少三项，一段起）、操作类（步骤链完整展开，可用编号列表或箭头链路，保留界面路径与注意点）、踩坑类（保留发现问题过程与修正思路）完整展开；观点类保持简洁，不注水。
- **素材类型契约**：大纲阶段为每条高价值素材标注【案例】【操作】【观点】【金句】【踩坑】【取舍】【疑问】前缀，作为章节生成阶段的展开契约。
- **素材密度标注**：每章标注素材密度（充足/中等/偏少）；主题重组分流导致的结构性薄章如实标注，不硬撑篇幅。
- **段落约束松绑**：叙述段 160-260 字；超 300 字优先拆分为多段，拆分是排版动作而非压缩许可，不得为压长度删案例要素或操作步骤；案例展开可连续多段推进。
- **讲者原话克制引用**：每章可保留 2-5 处原话金句（清理口水后 `>` 引用格式），提升密度感与讲者声音。
- **生成后内部自检**：章节完成后对照大纲素材清单逐项核对，案例/操作/踩坑类仅一句话带过的回到原文补足；自检不落盘。
- **模板去薄化**：实践案例模块模板改为"背景—过程—启发"多段示范；最佳实践模板移除与叙述性写作原则冲突的"首先其次最后"连接词示范。

### 文档完善

- 更新 `SKILL.md`、`references/outline_prompt.md`、`references/chapter_prompt.md`。
- 新增 `DECISIONS.md` [DEC-020]。

---

## [2.3.3] - 2026-05-06

### 修复

- **图片正文插入过度**：修正 v2.3.2 容易把图片资产表中的图片全部插入正文的问题，避免章节退化为幻灯片堆叠。
- **连续图片堆叠**：新增同一位置连续配图数量控制，原则上不超过 2 张，确有必要时最多 3 张。

### 改进

- **图片资产分级**：大纲阶段为每张图片标注 `核心图`、`辅助图`、`低价值图` 或 `跳过图`，并区分 `插入正文`、`仅资产表保留` 和 `跳过`。
- **正文配图克制**：保留图片资产表的完整追溯能力，但正文只插入能支撑核心观点、流程、案例或工具界面的高价值图片。
- **正文密度保护**：明确图片筛选只影响图片是否进入正文，不得同步压缩高价值观点、案例链路、工具取舍、真实疑问和实践细节。

### 文档完善

- 更新 `SKILL.md`、`references/outline_prompt.md`、`references/overview_prompt.md`、`references/chapter_prompt.md`。
- 同步 `README.md` 与 `.claude-plugin/marketplace.json` 中的 course-generator 版本和描述。

---

## [2.3.2] - 2026-05-01

### 修复

- **图片引用保真**：修复生成模式中原始 Markdown 图片引用容易在总览和章节生成阶段丢失的问题。
- **图片顺序稳定**：新增图片资产表，要求按原文顺序为图片分配 `IMG-xxx` 编号，并按章节归属保持顺序插入正文。
- **图片归属规则**：要求根据图片邻近时间戳、前后文和主题判断归属章节；无法判断时放入时间位置最接近的章节，不直接删除。

### 文档完善

- 更新 `SKILL.md`、`references/outline_prompt.md`、`references/overview_prompt.md`、`references/chapter_prompt.md`。
- 同步 `README.md` 与 `.claude-plugin/marketplace.json` 中的 course-generator 版本和描述。

---

## [2.3.1] - 2026-05-01

### 修复

- **独立正文去来源痕迹**：修正 v2.3.0 高保真正文增强后容易反复出现"现场演示""课程现场""现场问答"等来源叙述的问题。
- **操作过程正文化**：要求将演示、问答和补充讨论转化为实践案例、应用场景、常见疑问或操作流程，而不是在章节中保留转录现场痕迹。
- **英文专有名称保真**：明确保留 Skill 名称、工具名、项目名、产品名、命令名、API、文件名和框架名的原始英文写法，避免将 `Course Generator` 等专有名称意译成中文。

### 文档完善

- 更新 `SKILL.md`、`references/outline_prompt.md`、`references/overview_prompt.md`、`references/chapter_prompt.md`、`config/user_dictionary.example.yaml`。
- 同步 `README.md` 与 `.claude-plugin/marketplace.json` 中的 course-generator 版本和描述。

---

## [2.3.0] - 2026-05-01

### 改进

- **高保真正文增强**：生成模式明确采用书稿素材取向，不再只做轻量摘要，要求少压缩、多保留有思想密度的观点、案例、判断、金句和实践细节。
- **价值抽取标准强化**：大纲生成阶段新增高价值素材识别，覆盖方法论、工具选择理由、踩坑经验、反直觉判断、可迁移表达和现场问答中的真实疑惑。
- **章节正文增强**：专题文档要求将案例背景、问题、做法、结果和启发写完整，并说明工具或方案取舍的适用条件与局限。
- **问答融入规则固化**：Q&A 必须作为正文素材按主题融入章节，不单独成章，也不机械保留问答格式。

### 文档完善

- 更新 `SKILL.md`、`references/outline_prompt.md`、`references/overview_prompt.md`、`references/chapter_prompt.md`。
- 同步 `README.md` 与 `.claude-plugin/marketplace.json` 中的 course-generator 描述和版本号。

---

## [2.2.0] - 2026-05-01

### 改进

- **结构判断优化**：生成模式要求识别主讲内容、现场演示、问答和补充讨论四类素材，按主题组织章节并输出章节边界说明。
- **问答融合规则**：Q&A 不再作为独立章节处理，而是融入对应正文，转化为常见疑问、补充解释、案例回应或实践注意事项。
- **可读性优化**：专题文档要求三级标题使用编号格式，控制段落长度，避免长段堆叠。
- **结构导览改为可选**：总览仅在素材确实包含流程链路、方法框架、能力模型或系统架构时加入导览，不固定生成工作流地图。

### 文档完善

- 更新 `SKILL.md`、`references/outline_prompt.md`、`references/overview_prompt.md`、`references/chapter_prompt.md`。
- 同步 `README.md` 与 `.claude-plugin/marketplace.json` 中的 course-generator 描述和版本号。

---

## [2.1.0] - 2026-04-30

### 新增

- **用户词典术语纠错**：生成模式支持读取 `config/user_dictionary.yaml` 中的正确术语列表，在转录稿中识别高置信的近似误转写，并统一改为词典中的正确写法。
- **词典模板**：新增 `config/user_dictionary.example.yaml`，用户只需维护正确术语，不需要维护错误词到正确词的映射表。

### 改进

- 更新 `SKILL.md`：在生成模式中增加用户词典读取、近似误转写校正和低置信不替换规则。
- 更新 `references/outline_prompt.md`、`references/overview_prompt.md`、`references/chapter_prompt.md`：贯穿术语校正和术语一致性要求，避免后续生成阶段恢复转录错误。
- 更新 `README.md` 与 `.claude-plugin/marketplace.json` 中的 course-generator 描述和版本号。

---

## [2.0.1] - 2026-04-24

### 修复

- **路径配置改为绝对路径优先**：补充 `config/paths.example.yaml` 模板说明，明确建议在 `config/paths.yaml` 中使用绝对路径，避免因当前工作目录不同导致找不到知识库、课程体系和培训目录
- **兼容旧版总览命名**：提取模式和归档模式统一优先匹配 `00 [课程名称] - 总览.md`，同时兼容旧命名 `00_*.md`

### 文档完善

- 更新 `SKILL.md`：补充首次使用配置步骤、路径解析规则、总览文件命名兼容规则
- 更新 `references/extract_prompt.md`：补充绝对路径要求和总览文件匹配规则
- 同步 `README.md` 与 `.claude-plugin/marketplace.json` 中的 skill 描述和版本号

---

## [2.0.0] - 2026-04-23

### 重大更新

- **三模式架构**：从单一课程生成工具升级为课程内容管理平台
  - **生成模式**（保留）：转录稿/文献 → 结构化课程，新增自动归档
  - **归档模式**（新增）：将课程文件按 `YYMMDD 主题/` 规范整理到知识库
- **提取模式**（新增）：根据客户培训需求，从现有素材中提取重组课程方案
- **配置系统**：新增 `config/paths.yaml`，可配置知识库路径、归档命名规范、课程主题体系路径
- **课程主题体系**：通过 `curriculum_file` 配置指向外部课程体系文件，不再内嵌到 skill 中

### 新增文件

- `config/paths.example.yaml` — 路径配置模板
- `config/paths.yaml` — 本地路径配置（不提交到仓库）
- `references/extract_prompt.md` — 提取模式提示词

### 改进

- 生成模式完成后自动进入归档流程（可跳过）
- 模式自动识别：根据用户输入判断使用哪种模式
- 版本号从 1.3.1 升级到 2.0.0

---

## [1.3.1] - 2026-02-10

### 修复

- **移除内容来源归属表述**：修复生成课程内容时频繁出现"主讲人强调"、"主讲人指出"、"主讲人提到"等表述的问题
  - 更新核心原则：明确要求去除发言人标记，将内容直接呈现而非强调说话者
  - 在所有提示词模板中添加"客观陈述"要求
  - 将转录内容转换为客观的知识陈述形式，而非会议记录形式

### 改进

- **强化自然表达要求**：
  - 禁止使用"主讲人强调"、"主讲人指出"、"主讲人提到"、"主讲人认为"、"发言人表示"等表述
  - 直接陈述内容本身，不强调内容来源归属
  - 去除发言人标记，将内容整合为连贯的叙述

### 技术优化

- 更新 `skill.md` (2处)：核心原则中的"智能处理"和质量标准中的"自然表达"
- 更新 `references/chapter_prompt.md`：完全重写"自然表达"条款，增加4条具体子要求
- 更新 `references/outline_prompt.md`：在"内容优化"部分添加去除发言人标记的要求
- 更新 `references/overview_prompt.md`：新增第4条核心要求"客观陈述"

---

## [1.3.0] - 2026-02-07

### 改进

- **可选模块架构**：将固定的5部分章节结构改为可选模块库
  - 提供 11 个可选模块（引言与概述、核心概念与原理、理论框架、历史背景、实践案例、应用场景、最佳实践、对比分析、未来展望、本章小结、延伸阅读）
  - AI 根据内容类型和特征智能选择合适的模块组合
  - 提供默认推荐组合（理论类、实践类、案例类、综合类）
  - 每章至少 2 个模块，遵循逻辑关系

### 技术优化

- 更新 `references/chapter_prompt.md` - 从固定结构改为可选模块架构
- 更新 SKILL.md - 反映新的可选模块设计
- 提升技能对不同类型内容的适应性

### 文档更新

- 更新 DECISIONS.md - 添加可选模块架构决策记录
- 更新 TASKS.md - 添加 v1.3.0 任务记录

---

## [1.2.0] - 2026-02-07

### 新增功能

- **智能内容识别**：自动检测发言人、时间戳、配图等特征，智能选择处理方式
- **总览文档输出**：新增 `00_课程名称-总览.md`，包含课程概述、学习目标、课程结构、学习路径建议
- **配图处理**：自动识别配图链接并合理安排到相关主题章节
- **内容优化**：清洗冗余信息，转换口语为书面语

### 改进

- **强化叙述性写作**：完全避免要点列表，使用段落形式，保持逻辑连贯
- **案例融合原则**：案例和展望内容智能融入相关主题章节，不单独成章
- **中文标题序号**：使用"一二三"标题序号，更符合中文阅读习惯
- **专题文档结构**：5部分结构（引言与概述、核心概念与原理、实践案例分析、应用场景与最佳实践、本章小结）
- **输出位置优化**：输出目录改在输入文件所在位置

### 文档更新

- 新增 `references/overview_prompt.md` - 总览文档生成提示词
- 更新 `references/outline_prompt.md` - 添加内容优化、案例融合原则
- 更新 `references/chapter_prompt.md` - 采用5部分结构，强化叙述性写作
- 更新 SKILL.md - 反映新的工作流程和功能
- 更新 DECISIONS.md - 添加 v4.3 升级决策记录

### 技术变更

- 版本基于 Command v4.3
- 工作流程从 5 步扩展到 7 步（增加智能识别和总览生成）
- 文件命名从 `XX_序号_标题.md` 改为 `00_总览.md` + `0X_序号_标题.md`

---

## [1.1.0] - 2026-02-07

### 改进

- **合并工作流程步骤**：将"整体分析"和"生成课程大纲"合并为一步
  - 减少不必要的中间输出（分析报告）
  - 减少 AI 调用次数，提升响应速度
  - 保持思维连贯性，避免上下文切换
  - 更符合人脑处理信息的自然方式

### 技术优化

- 删除 `references/analysis_prompt.md`，功能合并到 `outline_prompt.md`
- 简化工作流程：从 6 步减少到 5 步
- 优化提示词模板，直接输出课程大纲

### 文档更新

- 更新 SKILL.md，反映新的工作流程
- 更新 DECISIONS.md，添加解耦决策记录

---

## [1.0.0] - 2026-02-07

### 新增

- **初始版本发布**：从 extract-course.md Command 转换为标准 Skill 格式
- **课程生成功能**：从多个文献内容中系统化提取并生成结构化课程
  - 支持 1-100+ 个文献文件的处理
  - 自动进行整体主题识别和内容分类
  - 基于原文生成系统化课程大纲
  - 逐章节生成详细的段落式内容
- **核心原则**：
  - 忠实原文：保留文献中的核心观点、具体例子、重要表述，不添加文档外信息
  - 段落式写作：避免要点罗列，包含完整的论证和阐述
  - 详尽完整：每个章节包含核心观点、案例分析、重要表述、数据支撑、背景信息

### v1.0.0 工作流程

1. **读取文献文件**：使用 Glob 工具一次性读取指定目录下的所有文献文件
2. **整体分析内容**：调用 AI 模型进行整体主题识别和内容分类
3. **生成课程大纲**：基于分析结果生成完整课程大纲
4. **逐章节生成内容**：为每个章节生成详细的段落式内容
5. **创建输出目录**：以 AI 生成的课程名称创建文件夹
6. **保存章节文件**：为每个章节创建独立文件

### 提示词模板（v1.0.0）

- **references/analysis_prompt.md**：整体分析提示词
- **references/outline_prompt.md**：课程大纲生成提示词
- **references/chapter_prompt.md**：章节内容撰写提示词

### 文档完善

- 添加 MIT License（LICENSE.txt）
- 创建 SKILL.md 主文件，包含完整工作流程说明
- 创建 CHANGELOG.md 版本变更记录
- 创建 DECISIONS.md 决策记录
- 创建 TASKS.md 任务管理

### 技术优化

- 使用 Progressive Disclosure 设计原则，将详细提示词移至 references/ 目录
- 保持 SKILL.md 简洁，便于快速理解和执行
- 模块化提示词设计，便于后续迭代优化

### 待办事项

- 收集用户使用反馈，优化提示词模板
- 考虑添加更多输出格式选项（如 PDF、Word）
- 考虑添加课程封面生成功能

---

## 原始版本记录（Command 时期）

### v4.3 (2025-12-15)
- 体验优化：输出目录改在输入文件所在位置，添加标题序号（一二三、123），进一步优化叙述性写作

### v4.2 (2025-12-15)
- 内容优化：强化叙述性写作，生成可直接用于书籍出版的章节内容，避免要点式罗列

### v4.1 (2025-12-15)
- 流程简化：统一处理逻辑，自动识别内容类型，无需区分标准文献和转录内容

### v4.0 (2025-12-15)
- 优化升级：增强内容处理能力，智能识别发言人、时间戳、配图等特征，默认生成总览和专题文档

### v3.2 (2025-11-21)
- 优化表达方式：移除"原文中"等生硬表述，使内容表达更加自然流畅

### v3.1 (2025-11-14)
- 通用化优化：支持1-100+个文献，生成系统化课程章节

### v3.0 (2025-11-14)
- 重大简化：文件从500行缩短至195行，保留核心要点和流程

### v2.3 (2025-11-14)
- 优化输出目录命名：使用AI生成的课程名称作为文件夹名

### v2.2 (2025-11-14)
- 移除所有导航文档，仅保留核心内容章节

### v2.1 (2025-11-14)
- 优化读取方式：一次性读取所有文件，保持完整上下文

### v2.0 (2025-11-14)
- 严格要求忠实于原文，改为信息整理导向

### v1.0 (2025-11-14)
- 初始版本
